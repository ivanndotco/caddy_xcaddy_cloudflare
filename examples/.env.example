# Cloudflare Credentials
# Required for DNS-01 ACME challenge with Cloudflare
#
# How to get these credentials:
# 1. Log in to Cloudflare Dashboard (https://dash.cloudflare.com/)
# 2. Go to My Profile > API Tokens
# 3. Create Token with "Edit zone DNS" template OR use Global API Key
# 4. Save the API Token
#
# Recommended: Use API Token (more secure)
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# Alternative: Use Global API Key (less secure, not recommended)
# CLOUDFLARE_EMAIL=your-email@example.com
# CLOUDFLARE_API_KEY=your_global_api_key_here

# Domain Configuration
DOMAIN=example.com

# WordPress Configuration (if using WordPress example)
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=your_secure_password_here
DB_ROOT_PASSWORD=your_root_password_here

# Grafana Configuration (if using monitoring example)
GRAFANA_USER=admin
GRAFANA_PASSWORD=your_grafana_password_here

# Notes:
# - Never commit this file with real credentials to git
# - Copy this file to .env and fill in your real values
# - The .env file is already in .gitignore
# - API Token is recommended over Global API Key for better security
# - Ensure your API token has "Zone:DNS:Edit" permissions for your domain
